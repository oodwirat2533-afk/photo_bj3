{
  "name": "photo-school-gas-webapp",
  "version": "1.0.0",
  "description": "School Photo Gallery & Management System on Vercel and Google Drive",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build complete'",
    "deploy": "npx @google/clasp push --force && npx @google/clasp deploy",
    "push": "git add . ; git commit -m \"update web app\" ; git push origin main"
  },
  "keywords": [
    "school-gallery",
    "photo-management",
    "vercel",
    "github"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@vercel/node": "^5.0.0"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
