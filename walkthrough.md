# อัปเดตสิทธิ์การใช้งานและการจัดการโฟลเดอร์

ระบบได้รับการพัฒนาเพิ่มเติมตามเงื่อนไขเพื่อความปลอดภัยของโฟลเดอร์หลัก เพิ่มความสะดวกในการจัดระเบียบรูปภาพ และพัฒนาประสบการณ์การใช้งานให้ลื่นไหลมากยิ่งขึ้น

## สิ่งที่เปลี่ยนแปลงในอัปเดตนี้

1. **ระบบป้องกันการอัปโหลดไฟล์ชื่อซ้ำ (ใหม่!) 🛡️**:
   - หากผู้ใช้อัปโหลดไฟล์ที่มี "ชื่อซ้ำ" กับไฟล์เดิมที่มีอยู่ในโฟลเดอร์นั้นๆ อยู่แล้ว ระบบจะทำการข้ามไฟล์นั้นไป (ไม่ให้อัปโหลดทับ) พร้อมแสดงหน้าต่างแจ้งเตือนให้ผู้ใช้ทราบว่ามีไฟล์ไหนที่ถูกข้ามไปบ้าง เพื่อป้องกันความสับสนและป้องกันไฟล์ซ้ำซ้อนในระบบ

2. **เพิ่มหน้าต่างแจ้งสถานะขณะอัปโหลด ⏳**:
   - เมื่อกดอัปโหลดไฟล์ หน้าจอจะแสดงหน้าต่างโหลด (Loading Overlay) พร้อมจำนวนไฟล์ที่กำลังอัปโหลด เพื่อให้คุณทราบว่าระบบกำลังทำงานอยู่ และป้องกันการเผลอไปกดปุ่มอื่นหรือปิดหน้าเว็บก่อนที่ไฟล์จะถูกส่งเสร็จ

3. **รองรับการอัปโหลดวิดีโอ (ไฟล์ขนาดใหญ่มาก) 🎥**:
   - ปรับปรุงกระบวนการอัปโหลดเบื้องหลังใหม่ทั้งหมด (Direct Resumable Upload) ทำให้สามารถอัปโหลดไฟล์วิดีโอขนาดใหญ่ (ทะลุข้อจำกัด 4.5 MB ของเซิร์ฟเวอร์เดิม) ได้สำเร็จโดยไม่เกิด Error อีกต่อไป!

4. **เพิ่มการอัปโหลดทีละหลายไฟล์ (Batch Upload) 🚀**:
   - ตอนนี้คุณสามารถคลุมดำ หรือกด Ctrl ค้างไว้เพื่อเลือกรูปภาพและวิดีโอ **หลายๆ ไฟล์พร้อมกัน** แล้วกดอัปโหลดในรอบเดียวได้แล้วครับ ไม่ต้องเสียเวลากดอัปโหลดทีละรูปอีกต่อไป!

5. **รีเฟรชข้อมูลอัตโนมัติเมื่อลบไฟล์/โฟลเดอร์ 🔄**:
   - ปรับปรุงให้เมื่อมีการลบโฟลเดอร์หรือรูปภาพ ระบบจะทำการดึงข้อมูลใหม่จาก Google Drive โดยอัตโนมัติ เพื่อให้หน้าเว็บแสดงผลข้อมูลที่ถูกต้องและเป็นปัจจุบันที่สุดทันทีหลังลบ

6. **เพิ่มสิทธิ์การลบโฟลเดอร์ (ใหม่! 🗑️)**:
   - ตอนนี้ **Superadmin** สามารถกดปุ่มลบ (ถังขยะ) ที่โฟลเดอร์ได้แล้ว (เพื่อความเป็นระเบียบและจัดการข้อมูลที่ไม่ได้ใช้)
   - เมื่อกดลบ ระบบจะลบโฟลเดอร์และไฟล์ที่อยู่ในนั้นออกจาก Google Drive โดยอัตโนมัติ

7. **เพิ่มฟีเจอร์ "สร้างโฟลเดอร์" 📁**:
   - เพิ่มปุ่ม "สร้างโฟลเดอร์" เข้ามาในระบบ โดยการกดจะแสดงหน้าต่างให้พิมพ์ชื่อโฟลเดอร์ที่ต้องการ
   - โฟลเดอร์จะถูกสร้างขึ้นใน Google Drive ให้ทันทีแบบอัตโนมัติ!

8. **จำกัดสิทธิ์หน้าแรกสุด (Root Folder) อย่างเข้มงวด 🔒**:
   - **ซ่อนปุ่มอัปโหลดทั้งหมด**: จะไม่มีใครสามารถอัปโหลดไฟล์รูปลอยๆ ทิ้งไว้ในหน้าแรกสุดได้อีกต่อไป ทุกคนจะต้องกดเข้าไปในโฟลเดอร์ใดโฟลเดอร์หนึ่งก่อน ถึงจะปรากฏปุ่มอัปโหลดให้เห็น
   - **จำกัดการสร้างโฟลเดอร์**: ที่หน้าแรกสุด จะมีเฉพาะ **Superadmin** เท่านั้น ที่มองเห็นปุ่ม "สร้างโฟลเดอร์" (Admin และคนอื่นๆ จะเห็นแค่รายการโฟลเดอร์ที่มีอยู่และทำได้แค่กดเข้าไปดูเท่านั้น)

9. **สิทธิ์การทำงานในโฟลเดอร์ย่อย**:
   - เมื่อกดเข้ามาในโฟลเดอร์ย่อยแล้ว **Admin** จะได้รับอนุญาตให้กดปุ่ม "สร้างโฟลเดอร์" ย่อยข้างในต่อได้
   - **ผู้ช่วย Admin** จะเห็นเพียงปุ่มอัปโหลดรูปตามปกติ ไม่สามารถสร้างโฟลเดอร์และลบรูปได้
