# ย้ายระบบจัดการมายังหน้าหลัก

ระบบคลังภาพได้รับการอัปเกรดให้สามารถใช้งานได้จบในหน้าเดียว (`/`) โดยไม่ต้องมีหน้า `/admin` แยกต่างหากอีกต่อไป

## สิ่งที่เปลี่ยนแปลง

1. **ยุบรวมระบบจัดการ**: โค้ดทั้งหมดจาก `app/admin/page.tsx` ถูกย้ายมาไว้ที่หน้าหลัก `app/page.tsx` และหน้า `/admin` ได้ถูกลบออกไปจากระบบ
2. **ปรับปรุงส่วนหัว (Header)**:
   - ผู้ใช้ที่ยังไม่ได้ล็อกอิน จะเห็นปุ่ม "เข้าสู่ระบบ (Admin)" 
   - เมื่อล็อกอินแล้ว จะแสดงชื่อบัญชี, อีเมล, **บทบาท (Role)** และปุ่ม "ออกจากระบบ"
3. **การเข้าถึงตามสิทธิ์ (Role-Based Access)**:
   - **Superadmin**: เมื่อล็อกอินเข้ามา จะมีแถบพิเศษ **"⚙️ ตั้งค่าระบบคลังภาพ (เฉพาะ Superadmin)"** แสดงขึ้นมาใต้แถบชื่อ โดยสามารถกดเพื่อกางออกดูตั้งค่า URL ของไดรฟ์ และจัดการรายชื่อผู้ใช้ได้
   - **Admin / ผู้ช่วย Admin**: จะไม่เห็นแถบการตั้งค่าใดๆ แต่จะเห็นปุ่ม **"อัปโหลดไฟล์"** และสามารถจัดการรูปภาพได้ตามปกติบนหน้าเดียวกัน
4. **การ Redirect แบบใหม่**: เมื่อผู้ใช้ใหม่ได้รับการเชิญแล้วทำการล็อกอินเข้ามา จะถูกส่งไปที่หน้า `/onboarding` เพื่อกรอกข้อมูล เมื่อกรอกเสร็จระบบจะนำทางกลับมาที่หน้าหลักให้โดยอัตโนมัติ
