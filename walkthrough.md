# เปลี่ยนรูปแบบเป็นเมนูด้านบน (Top Navbar)

ระบบได้รับการปรับปรุงหน้าตาใหม่เพื่อให้เข้าถึงส่วนต่างๆ ได้ง่ายขึ้น โดยแยกแต่ละหัวข้อออกเป็นหน้าอย่างชัดเจน

## สิ่งที่เปลี่ยนแปลงในอัปเดตนี้

1. **เพิ่มแถบเมนูด้านบน (Top Navigation Bar)**:
   - แถบเมนูนี้จะติดอยู่ด้านบนของทุกหน้าตลอดเวลา
   - มีการปรับให้รองรับการใช้งานทั้งบนคอมพิวเตอร์และมือถือ (มือถือจะมีปุ่มสามขีดสำหรับเปิดเมนู)
   - ข้อมูลบัญชีผู้ใช้และปุ่ม "ออกจากระบบ" จะถูกย้ายมาไว้ที่แถบนี้

2. **แยกหน้าการตั้งค่า**:
   - **คลังภาพ (`/`)**: หน้าหลักที่แสดงโฟลเดอร์และรูปภาพ พร้อมปุ่มอัปโหลดสำหรับ Admin
   - **ตั้งค่าลิงก์ Google Drive (`/settings/drive`)**: หน้าใหม่สำหรับให้ Superadmin มาตั้งค่าโฟลเดอร์หลักโดยเฉพาะ
   - **จัดการผู้ดูแลระบบ (`/settings/users`)**: หน้าใหม่ที่แยกออกมาให้ Superadmin เข้ามาเพิ่ม/ลบ และปรับเปลี่ยนบทบาทผู้ดูแลระบบ

3. **ความปลอดภัย**:
   - หากผู้ใช้ที่ไม่ใช่ Superadmin พยายามจะเข้าหน้าตั้งค่าต่างๆ ผ่าน URL (เช่น พิมพ์ `/settings/users` โดยตรง) ระบบหลังบ้านจะบล็อกและดีดกลับมาที่หน้าคลังภาพให้โดยอัตโนมัติ
