# แผนการสร้างฟีเจอร์จัดการโฟลเดอร์และจำกัดสิทธิ์หน้าหลัก

## สิ่งที่ต้องทำ (Proposed Changes)

1. **เพิ่ม API สร้างโฟลเดอร์ (`app/api/drive/create-folder/route.ts`)**:
   - สร้าง API ใหม่เพื่อคุยกับ Google Drive ในการสร้างโฟลเดอร์
   - เพิ่มระบบรักษาความปลอดภัย:
     - หากเป็นการสร้างใน "โฟลเดอร์หลัก (Root)" จะอนุญาตเฉพาะผู้ที่มีสิทธิ์ `superadmin`
     - หากเป็นโฟลเดอร์ย่อย จะอนุญาตทั้ง `superadmin` และ `admin`

2. **ปรับปรุงหน้าหลัก (`app/page.tsx`)**:
   - **ปิดปุ่มอัปโหลดหน้าหลัก**: ถ้าผู้ใช้อยู่ใน "โฟลเดอร์หลัก" จะซ่อนปุ่ม "อัปโหลดไฟล์" ทันที (ทุกคนจะไม่สามารถอัปโหลดไฟล์ได้ในหน้านี้ ต้องเข้าไปในโฟลเดอร์ย่อยก่อน)
   - **เพิ่มปุ่ม "สร้างโฟลเดอร์"**: 
     - ในโฟลเดอร์หลัก: ปุ่มนี้จะขึ้นเฉพาะบัญชี `superadmin` เท่านั้น
     - ในโฟลเดอร์ย่อย: ปุ่มนี้จะขึ้นให้ทั้ง `superadmin` และ `admin`
   - **สร้างหน้าต่าง Popup**: เมื่อกดปุ่ม "สร้างโฟลเดอร์" จะมีหน้าต่างขึ้นมาให้พิมพ์ชื่อโฟลเดอร์และกดยืนยัน

## User Review Required

> [!IMPORTANT]
> สรุปสิทธิ์ที่จะเกิดขึ้น:
> - **หน้าแรกสุด**: 
>   - **Superadmin**: สร้างโฟลเดอร์ได้
>   - **Admin / ผู้ช่วย Admin**: ทำอะไรไม่ได้นอกจากกดเข้าไปในโฟลเดอร์ที่มีอยู่
> - **โฟลเดอร์ย่อย (เมื่อกดเข้าไปแล้ว)**:
>   - **Superadmin / Admin**: อัปโหลดไฟล์ได้, สร้างโฟลเดอร์ย่อยได้, ลบไฟล์ได้
>   - **ผู้ช่วย Admin**: อัปโหลดไฟล์ได้อย่างเดียว (สร้างโฟลเดอร์/ลบไม่ได้)
> 
> หากสิทธิ์ตามด้านบนนี้ถูกต้อง พิมพ์ "ตกลง" หรือกด Proceed เพื่อให้ผมเริ่มเขียนโค้ดได้เลยครับ!
