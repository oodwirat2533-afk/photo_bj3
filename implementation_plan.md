# แผนการแก้ไขปัญหาการอัปโหลดวิดีโอ (ไฟล์ขนาดใหญ่)

ปัญหาที่เกิดขึ้นเกิดจากข้อจำกัดของระบบเซิร์ฟเวอร์ Vercel ที่ไม่อนุญาตให้อัปโหลดไฟล์ผ่านเซิร์ฟเวอร์ที่มีขนาดใหญ่เกิน 4.5 MB (ซึ่งวิดีโอส่วนใหญ่จะใหญ่กว่านั้นมาก) ทำให้การส่งไฟล์ถูกตัดการเชื่อมต่อ

## วิธีการแก้ไข (Resumable Upload)
เราจะเปลี่ยนวิธีการอัปโหลด จากเดิมที่ส่งไฟล์ไปที่เซิร์ฟเวอร์เราก่อน (Client -> Next.js -> Google Drive)
เปลี่ยนเป็นการให้เบราว์เซอร์ **ส่งไฟล์ตรงเข้า Google Drive (Client -> Google Drive)** โดยตรง ซึ่งจะทะลุข้อจำกัด 4.5 MB และอัปโหลดไฟล์ขนาดใหญ่เป็น GB ได้เลย

## Proposed Changes

1. **สร้าง API ใหม่ `app/api/drive/init-upload/route.ts`**
   - API นี้จะทำหน้าที่รับข้อมูลชื่อไฟล์และโฟลเดอร์ปลายทาง
   - นำข้อมูลไปขอลิงก์พิเศษสำหรับอัปโหลด (Resumable Session URI) จาก Google Drive
   - ส่งลิงก์นี้กลับมาให้เบราว์เซอร์

2. **ปรับปรุงระบบหน้าบ้าน (Frontend) ใน `app/page.tsx`**
   - ในตอนที่ผู้ใช้กดอัปโหลด ระบบจะเรียกไปที่ API `init-upload` เพื่อขอลิงก์อัปโหลดก่อน
   - เมื่อได้ลิงก์มา ระบบจะทำการยิงไฟล์ (PUT Request) ตรงเข้าสู่เซิร์ฟเวอร์ของ Google Drive โดยตรง
   - ดึงข้อมูลไฟล์กลับมาแสดงผลที่หน้าจอตามปกติ

## User Review Required
> [!IMPORTANT]
> การปรับปรุงนี้จะทำให้สามารถอัปโหลดวิดีโอขนาดใหญ่ได้ไหลลื่นยิ่งขึ้น และลดภาระของเซิร์ฟเวอร์
> หากเห็นด้วยกับแนวทางแก้ไขนี้ กด **"ตกลง"** เพื่อให้ผมเริ่มดำเนินการเขียนโค้ดได้เลยครับ!
