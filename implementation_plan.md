# ย้ายการตั้งค่าระบบมาไว้ที่หน้าแรก (Front-page Settings Integration)

เป้าหมายคือการย้ายระบบจัดการทั้งหมด (การตั้งค่าลิงก์ Drive และการจัดการแอดมิน) ออกจากหน้า `/admin` แล้วนำมารวมไว้ที่หน้าหลัก (`/`) หน้าเดียว เพื่อความสะดวกในการใช้งาน โดยเมนูต่างๆ จะแสดงตามสิทธิ์ (บทบาท) ของผู้ใช้งานโดยอัตโนมัติ

## Proposed Changes

### 1. ไฟล์ `app/page.tsx` (หน้าหลัก)
ย้ายโค้ด UI และ Logic การทำงานจากหน้า Admin มารวมไว้ที่นี่:
- **ปุ่มเข้าสู่ระบบ / ออกจากระบบ**: 
  - หากยังไม่ล็อกอิน: แสดงปุ่ม "เข้าสู่ระบบ (Admin)"
  - หากล็อกอินแล้ว: แสดงชื่อ, บทบาท, และปุ่ม "ออกจากระบบ" ที่มุมบนขวา
- **สำหรับ Admin / ผู้ช่วย Admin**:
  - จะเห็นปุ่ม "อัปโหลดไฟล์" และสามารถจัดการรูปภาพได้ (ตามสิทธิ์ที่ตั้งไว้ก่อนหน้า)
- **สำหรับ Superadmin**:
  - จะมีปุ่ม/ส่วนที่ยุบขยายได้ (Accordion) ชื่อว่า **"⚙️ ตั้งค่าระบบ (เฉพาะ Superadmin)"**
  - เมื่อกดกางออก จะพบกับ 2 ส่วนเดิม:
    1. ฟอร์มตั้งค่าลิงก์โฟลเดอร์ Google Drive หลัก
    2. ส่วนเพิ่มและแก้ไขรายชื่อผู้ดูแลระบบ (ตารางรายชื่อ)
- Logic ในการทำงานทั้งหมด (เช่น `handleAddAdmin`, `handleDeleteAdmin`, `handleEditRole`, `handleUpdateUrl`) จะถูกนำมารวมไว้ที่หน้าหลัก

### 2. ไฟล์ `app/api/auth/[...nextauth]/route.ts`
- เปลี่ยนเส้นทางการ Redirect หลังจากเข้าสู่ระบบเสร็จสิ้น ให้กลับมาที่หน้าหลัก `/` แทนที่จะไปหน้า `/admin`

### 3. ไฟล์ `proxy.ts` (Middleware)
- ปรับปรุงการดักจับเส้นทาง (Routing) เนื่องจากไม่มีหน้า `/admin` แล้ว หากล็อกอินแล้วแต่ยังไม่ได้กรอกข้อมูล (Onboard) ก็จะยังคงถูกส่งไปหน้า `/onboarding` เช่นเดิมเมื่อเข้าหน้าหลัก

### 4. ลบไฟล์ที่ไม่ได้ใช้งาน
- **[DELETE]** `app/admin/page.tsx` เนื่องจากเราย้ายทุกอย่างไปไว้หน้าหลักแล้ว

## User Review Required

> [!IMPORTANT]
> - การย้ายข้อมูลทั้งหมดมาหน้าหลักจะทำให้หน้าหลักมีฟังก์ชันเยอะขึ้นสำหรับ Superadmin ผมจะทำเป็น **แถบตั้งค่าที่สามารถกดเปิด/ปิด (ย่อ/ขยาย) ได้** เพื่อไม่ให้รกหน้าจอจนเกินไป ดีไหมครับ?
> - สำหรับการอัปโหลดไฟล์ ปุ่มอัปโหลดจะยังคงแสดงในตำแหน่งเดิมสำหรับ Admin/ผู้ช่วย Admin ครับ

คุณเห็นด้วยกับแผนการนี้ไหมครับ? หากตกลงผมจะเริ่มดำเนินการรวมไฟล์ทันทีครับ
