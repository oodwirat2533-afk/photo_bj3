# ปรับปรุงโครงสร้างเมนูด้านบนและแยกหน้าการตั้งค่า

ตามที่คุณต้องการให้มีแถบเมนูด้านบน (Top Navigation Bar) และแยกส่วนการตั้งค่าออกเป็นหน้าต่างหาก เพื่อความสวยงามและเป็นระเบียบเรียบร้อย

## Proposed Changes

### 1. สร้างแถบเมนูด้านบน (Navbar)
- สร้าง Component `Navbar` ที่จะแสดงอยู่ด้านบนสุดของทุกๆ หน้าเสมอ
- **สำหรับบุคคลทั่วไป/นักเรียน**: จะเห็นแค่เมนู "คลังภาพ" และปุ่ม "เข้าสู่ระบบ"
- **สำหรับ Admin / ผู้ช่วย Admin**: จะเห็นเมนู "คลังภาพ" พร้อมแสดงชื่อผู้ใช้และปุ่ม "ออกจากระบบ"
- **สำหรับ Superadmin**: จะเห็นเมนูดังนี้
  - คลังภาพ (หน้าหลัก)
  - ตั้งค่าลิงก์ Google Drive
  - จัดการผู้ดูแลระบบ
  - (พร้อมแสดงชื่อและปุ่มออกจากระบบ)

### 2. แยกหน้าการทำงาน (Routing)
สร้างหน้าเพจใหม่แยกตามหัวข้อดังนี้:
- **`/` (หน้าหลัก)**: แสดงคลังภาพและวิดีโอ (อัปโหลดและลบได้ตามสิทธิ์)
- **[NEW] `/settings/drive`**: หน้าสำหรับตั้งค่าลิงก์โฟลเดอร์ Google Drive หลัก (เฉพาะ Superadmin)
- **[NEW] `/settings/users`**: หน้าสำหรับเพิ่ม/ลบ และแก้ไขบทบาทของผู้ดูแลระบบ (เฉพาะ Superadmin)

### 3. ปรับปรุงการป้องกันการเข้าถึง (Middleware)
- ปรับปรุงไฟล์ `proxy.ts` ให้ทำการบล็อกการเข้าถึงหน้า `/settings/...` หากผู้ใช้ไม่ใช่ Superadmin ระบบจะดีดกลับไปที่หน้าหลักทันที

### 4. ปรับหน้าต่างเข้าสู่ระบบ (Onboarding)
- หน้า Onboarding สำหรับการตั้งชื่อและนามสกุลจะยังคงทำงานเหมือนเดิม โดยแถบเมนูด้านบนในหน้านี้จะแสดงแบบเรียบง่าย ไม่ให้กดไปหน้าอื่นจนกว่าจะกรอกข้อมูลเสร็จ

## User Review Required

> [!IMPORTANT]
> ด้วยการแบ่งหน้าแบบนี้:
> 1. แถบเมนูด้านบนจะแสดงค้างไว้ตลอด เพื่อให้กดสลับหน้าไปมาได้ง่าย
> 2. สิทธิ์การเข้าถึงหน้าตั้งค่าจะถูกล็อกอย่างแน่นหนาผ่านระบบหลังบ้าน เพื่อให้มั่นใจว่ามีแค่ Superadmin เท่านั้นที่เข้าหน้า `/settings/...` ได้
> 
> คุณเห็นด้วยกับรูปแบบนี้ไหมครับ? หากตกลง พิมพ์ "ตกลง" หรือกด Proceed ได้เลยครับ ผมจะลงมือแยกหน้าให้ทันที!
